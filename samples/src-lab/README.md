# Authorized SRC lab

This directory is a local, fictional target for IMF demos. It is not a live system.

Scope: files in this folder only. Do not scan hosts outside the workplace.

Look for debug leftovers, hardcoded tokens, and error paths that print internal state.
